<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <!-- Fürs Diktat. Die Erlaubnis holt sich nicht der Dienst, sondern die
       durchsichtige Aktivität: eine Eingabemethode darf selbst keinen
       Erlaubnis-Dialog aufmachen. -->
  <uses-permission android:name="android.permission.RECORD_AUDIO"/>

  <!-- Ab Android 11 sieht eine App fremde Dienste nur, wenn sie danach
       fragt. Ohne diesen Block findet SpeechRecognizer nichts und meldet
       stumm einen Fehler. -->
  <queries>
    <intent>
      <action android:name="android.speech.RecognitionService"/>
    </intent>
  </queries>

  <application
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher"
      android:allowBackup="true"
      android:supportsRtl="true"
      android:theme="@style/ThemaEinrichtung">

    <service
        android:name=".TastaturDienst"
        android:label="@string/app_name"
        android:permission="android.permission.BIND_INPUT_METHOD"
        android:exported="true">
      <intent-filter>
        <action android:name="android.view.InputMethod"/>
      </intent-filter>
      <meta-data
          android:name="android.view.im"
          android:resource="@xml/methode"/>
    </service>

    <activity
        android:name=".EinrichtungActivity"
        android:exported="true"
        android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <!-- Ohne Fenster und ohne Übergang: sie fragt nach dem Mikrofon und
         verschwindet sofort wieder, damit die Tastatur sichtbar bleibt. -->
    <activity
        android:name=".BerechtigungActivity"
        android:exported="false"
        android:excludeFromRecents="true"
        android:noHistory="true"
        android:theme="@style/ThemaUnsichtbar"/>
  </application>
</manifest>
